# Dependencies
node_modules/

# Environment / secrets (chaves do Supabase vivem em env vars do Netlify/Vercel)
.env
.env.*
!.env.example

# Deploy / build output
.netlify/
.vercel/
dist/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Asset de 9 MB não referenciado pelo site. Remova esta linha para versioná-lo.
/abstract-elegant-blue-background-with-text-space.jpg

.vercel
.env*

# Contexto local de IA (repo publico - nao versionar)
CLAUDE.md
.agents/

# .claude/: versionamos so as skills do Spec Kit (uteis em qualquer maquina).
# O resto e local desta maquina e o repo e publico - nao pode vazar.
.claude/*
!.claude/skills/
.claude/settings.local.json
.claude/worktrees/
